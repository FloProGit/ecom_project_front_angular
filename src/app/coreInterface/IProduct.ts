export interface IProduct {
  id: number,
  name: string,
  ext_reference: string,
  url_link: string,
  ext_id: number,
  quantity: number,
  current_condition: string,
  price_tax_exclude:number

  description : string
}
