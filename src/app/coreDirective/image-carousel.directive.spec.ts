import { ImageCarouselDirective } from './image-carousel.directive';

describe('ImageCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
